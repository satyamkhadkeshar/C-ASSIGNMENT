
#include <stdio.h>

int main()
{
    float area,pi,circ,r;
    pi= 3.14;
    printf("enter the radius of circle");
    scanf("%f", &r);
    area= pi*r*r;
    circ= 2*pi*r;
    printf("circumference = %f", area);
    printf("area = %f", circ);
    return 0;
}
